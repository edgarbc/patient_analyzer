# Core dependencies (lightweight for CI)
numpy>=1.20.0
pandas>=1.3.0
scikit-learn>=0.24.2
streamlit>=1.0.0
matplotlib>=3.3.4

# Optional: For production-quality embeddings (heavy, not required for demo)
# Uncomment below for sentence-transformers support:
# sentence-transformers>=2.0.0
# torch>=1.9.0
# transformers>=4.0.0
